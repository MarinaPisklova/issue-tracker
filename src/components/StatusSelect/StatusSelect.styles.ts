import styled from 'styled-components';

export const StyledStatusSeelect = styled.select`
    background: white;
    color: gray;
    border: solid 1px gray;
    font-size: 1rem;
    text-align: center;
    padding: 0.5rem 1rem;
    border-radius: 0.25rem;
    font-weight: bold;
    cursor: pointer;
`;
